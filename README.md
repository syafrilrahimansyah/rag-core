# rag-core
A modular, lightweight Retrieval-Augmented Generation (RAG) engine built with Python, utilizing Milvus Standalone (Docker) for vector storage, SQLite for document metadata, and Ollama (Docker/Local) for local embedding generation and LLM completion.
